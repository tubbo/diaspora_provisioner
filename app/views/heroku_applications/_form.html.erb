<%= form_for @heroku_application, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= form_action_title %> Heroku Application</legend>
    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :heroku_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :heroku_id, :class => 'text_field' %>
      </div>
    </div>
    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', heroku_applications_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>